# Copy this to .env and fill in your Postgres connection details.
# Use a read-only DB role if possible — the app already runs everything
# in READ ONLY transactions, but a read-only role is a good extra layer.
DATABASE_URL=postgres://postgres:password@localhost:5432/postgres
